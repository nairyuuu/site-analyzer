// chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
//     if (message.type === "scanResult") {
//         chrome.storage.local.set({ scanData: message.data });
//     }
// });

